package browsers;

import java.time.Duration;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.chrome.ChromeOptions;
import org.openqa.selenium.edge.EdgeDriver;
import org.openqa.selenium.edge.EdgeOptions;
import org.openqa.selenium.firefox.FirefoxDriver;
import org.openqa.selenium.firefox.FirefoxOptions;
import driver.Driver;

public class Navegadores extends Driver {

    private static String navegadorEscolhido;

    public static void setNavegadorEscolhido(String navegador) {
        if (navegador.equalsIgnoreCase("chrome") || navegador.equalsIgnoreCase("firefox") || navegador.equalsIgnoreCase("edge")) {
            navegadorEscolhido = navegador.toLowerCase();
        } else {
            throw new IllegalArgumentException("Navegador inválido: " + navegador);
        }
   		driver.manage().timeouts().implicitlyWait(Duration.ofSeconds(10));
		driver.manage().window().maximize();
	
	}
}